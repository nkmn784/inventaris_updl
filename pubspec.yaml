name: inventaris_updl
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^3.1.0
  cloud_firestore: ^5.1.0
  firebase_auth: ^5.1.0
  url_launcher: ^6.3.2
  excel: ^4.0.6
  path_provider: ^2.1.6
  open_file: ^4.0.0
  image_picker: ^1.2.3
  http: ^1.6.0
  geolocator: ^14.0.3
  geocoding: ^5.0.0
  firebase_storage: ^12.4.10
  csv: ^6.0.0
  share_plus: ^13.3.0
  file_saver: ^0.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true